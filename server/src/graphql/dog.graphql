type Dog {
  _id: String!
  name: String!
  color: String
  age: Int
  owner: Owner
}

input CreateDogInput {
  _id: String
  name: String
  color: String
  age: Int
  owner: String
}

type Query {
  getAllDog: [Dog]
}

type Mutation {
  addDog(dog: CreateDogInput): Dog
  deleteDog(dog: CreateDogInput): Boolean
  updateDog(dog: CreateDogInput, udog: CreateDogInput): Boolean
}
