type Use {
  id: Int
  recipeId: Int
  recipe: Recipe
  metarialId: Int
  metarial: Metarial
}

type Query {
  getAllUse: [Use]
}

type Mutation {
  createUse(recipeId: Int, metarialId: [Int]): [Use]
  updateUse(recipeId: Int, metarialId: [Int]): [Use]
}
